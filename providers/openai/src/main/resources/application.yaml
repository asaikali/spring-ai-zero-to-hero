spring:
  config:
    import: "optional:classpath:/creds.yaml"
  ai:
    openai:
      chat:
        base-url: https://api.openai.com/v1/chat/completions
        options:
          functions:
            - weatherFunction
          n: 3
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"

